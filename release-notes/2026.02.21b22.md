Add explicit v2 trait-inventory diagnostics for Nest auth and guest surface discovery.

## Highlights
- Record and expose the raw v2 trait-descriptor inventory seen on observe responses, including structure-level descriptors.
- Log explicit empty supplemental-auth results so `extra_auth_traits=0` is confirmed by a matching debug line instead of silence.
- Keep passcode safety behavior unchanged while improving evidence for whether Nest exposes any alternate guest/auth write surface on this account.

## Validation
- `python3 -m compileall custom_components/nest_yale_lock`
- `python3 -m unittest tests/test_passcode_crypto.py tests/test_passcode_utils.py`

## Breaking changes
- None.
