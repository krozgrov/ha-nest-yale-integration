Deepen ApplicationKeysTrait nested key scan for passcode candidate discovery.

## Highlights
- Increase recursive scan depth for nested `ApplicationKeysTrait` key blobs (`32`/`36` byte candidates) to catch deeply wrapped key material variants.
- Keep passcode safety behavior unchanged: updates are still blocked unless candidate material validates against existing encrypted pincodes.

## Validation
- `python3 -m unittest tests/test_passcode_crypto.py tests/test_passcode_utils.py`
- `python3 -m compileall custom_components/nest_yale_lock`

## Breaking changes
- None.
