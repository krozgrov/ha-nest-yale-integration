Request Nest guest and user-access traits in the observe stream for passcode-path discovery.

## Highlights
- Expand the v2 observe subscription list to explicitly request `nest.trait.guest.GuestsTrait`, `nest.trait.user.UserAccessTrait`, and `weave.trait.schedule.BasicUserSchedulesSettingsTrait`.
- Log the exact observe trait subscriptions so field testing can confirm the client is asking Nest for the guest-related surfaces we want to inspect.
- Keep validated-only passcode safety gates unchanged while improving visibility into whether a structure-level guest command path is exposed.

## Validation
- `python3 -m compileall custom_components/nest_yale_lock`
- `python3 -m unittest tests/test_passcode_crypto.py tests/test_passcode_utils.py`

## Breaking changes
- None.
