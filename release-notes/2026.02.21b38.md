Fix firmware version display in Home Assistant Device Info when Nest reports `softwareVersion`.

- Normalize firmware metadata from `softwareVersion` / `swVersion` style fields in addition to existing firmware keys.
- Normalize DeviceIdentityTrait firmware values across more protobuf field-name variants.
- Add regression coverage for metadata and protobuf firmware decoding.
