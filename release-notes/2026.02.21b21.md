Expose guest and auth trait diagnostics to investigate alternate Nest passcode paths.

## Highlights
- Preserve and surface all observed non-`ApplicationKeysTrait` `weave.trait.auth.*` payloads even when they do not yield immediate key candidates.
- Decode `nest.trait.guest.GuestsTrait` summaries so diagnostics and logs can confirm whether Nest is exposing a guest-management surface alongside the lock traits.
- Keep passcode safety behavior unchanged: validated-only updates remain enforced, and writes still abort when no candidate material matches existing encrypted pincodes.

## Validation
- `python3 -m compileall custom_components/nest_yale_lock`

## Breaking changes
- None.
