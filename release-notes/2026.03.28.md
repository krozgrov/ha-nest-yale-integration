Fix Home Assistant area ownership, protobuf coexistence, and startup metadata reliability.

- Stop creating or reassigning Home Assistant areas from Nest `where_label`.
- Reuse shared protobuf modules when compatible schemas are already installed, avoiding duplicate descriptor import failures.
- Normalize additional protobuf and metadata field aliases so firmware and lock companion data populate correctly.
- Improve startup backfill timing and defer companion entities until source values exist, reducing temporary unavailable states.
